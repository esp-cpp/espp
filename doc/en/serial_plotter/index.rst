Serial Plotter APIs
*******************

.. toctree::
    :maxdepth: 1

    serial_plotter

The `serial_plotter` component is a browser data plotter (Web Serial text/CSV +
WebUSB binary) with an optional firmware-side `Telemetry` emitter that streams
typed float channels the web app plots live.
